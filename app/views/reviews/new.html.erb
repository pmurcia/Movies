<%= form_for [@movie, @review] do |f|%>
	<%= f.label :reviewer %>
	<%= f.text_field :reviewer%>
	<% if @review.errors.any? %>
	  <% @review.errors[:reviewer].each do |error_msg| %>
	    <%= error_msg %>
	  <% end %>
	<% end %>
	<br>
	<%= f.label :content %>
	<%= f.text_field :content %>
	<% if @review.errors.any? %>
	  <% @review.errors[:content].each do |error_msg| %>
	    <%= error_msg %>
	  <% end %>
	<% end %>
	<br>

	<%= f.submit 'Save'%>
<% end %>